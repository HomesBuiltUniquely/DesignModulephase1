# OPTIONAL — only if Nginx returns 413/502 *before* Node and the browser only shows a CORS error.
# Add inside  http { ... }  in /etc/nginx/nginx.conf, then ask your DevOps to add matching
# add_header ... always; lines (avoid `if` + add_header in the same block — nginx quirks).
#
# map $http_origin $cors_allow_origin {
#     default "";
#     "https://design.hubinterior.com"       $http_origin;
#     "https://www.design.hubinterior.com"   $http_origin;
# }
